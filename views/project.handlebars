<div class="text-center">
  <h2>{{name}}</h2>
  <p>{{{get_emoji}}}</p>
  <p>{{description}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <p>
    <span class="dollar-amount">{{format_amount travel_year}}</span>
    needed
  </p>
  <p>
    <button class="btn btn-primary">Plan this trip!</button>
  </p>
</div>
